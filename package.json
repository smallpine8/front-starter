{
  "name": "front-starter",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "config": {
    "port": 80
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:server": "browser-sync start --config bs-config.js",
    "dev:ejs": "ejs-cli -b _src/html/pages/ \"**/*.ejs\" -e \"_src/html/partials/\" -o _public"
  },
  "devDependencies": {
    "browser-sync": "^2.26.14",
    "ejs-cli": "^2.2.1",
    "npm-run-all": "^4.1.5"
  }
}
